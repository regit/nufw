# Automatically added by dh_installinit
if [ -x "/etc/init.d/nufw" ]; then
	if [ -x /usr/sbin/invoke-rc.d ] ; then
		invoke-rc.d nufw stop || exit 0
	else
		/etc/init.d/nufw stop || exit 0
	fi
fi
# End automatically added section
