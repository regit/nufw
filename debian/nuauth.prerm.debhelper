# Automatically added by dh_installinit
if [ -x "/etc/init.d/nuauth" ]; then
	if [ -x /usr/sbin/invoke-rc.d ] ; then
		invoke-rc.d nuauth stop || exit 0
	else
		/etc/init.d/nuauth stop || exit 0
	fi
fi
# End automatically added section
