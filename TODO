# Partial TODO List

- nuauth : cache : configurable max number of cached entries
                   configurable max number of cached entries per protocol?

- nuauth : check for simultaneous login of same user

- nuauth : do not send hello message if multiple instance runs on the same computer

# Modules
- libdb -> load all in cache and close the file straight away

# TLS
password protected certificat support
