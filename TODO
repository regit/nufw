# Partial TODO List

# gw pckt srv
- use PRIO (set option and decide for packet when as high as needed)


# User auth srv
- use id server and authgw
- down ldap timeout to less than packet timeout
- set packet timeout to less than proto timeout
- config file
- plugin structure for external auth module
